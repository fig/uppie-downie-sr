<% unless user_signed_in? %>
  <%= link_to t("devise.sessions.log_in"), new_user_session_path %>
  <%= link_to t("devise.registrations.register_now"), new_user_registration_path %>
<% else %>
  <%= link_to t("devise.registrations.my_account"), edit_user_registration_path %>
  <%= link_to t("devise.sessions.log_out"), destroy_user_session_path, method: :delete %>
<% end %>
